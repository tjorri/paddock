apiVersion: apps/v1
kind: Deployment
metadata:
  name: broker
  namespace: system
  labels:
    app.kubernetes.io/name: paddock
    app.kubernetes.io/component: broker
    app.kubernetes.io/managed-by: kustomize
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: paddock
      app.kubernetes.io/component: broker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: paddock
        app.kubernetes.io/component: broker
    spec:
      serviceAccountName: broker
      terminationGracePeriodSeconds: 10
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: broker
          # Literal here; hack/gen-helm-chart.sh rewrites this into a
          # {{ .Values.brokerImage ... }} template when rendering the chart.
          image: paddock-broker:dev
          command: ["/broker"]
          args:
            - --listen-address=:8443
            - --health-probe-bind-address=:8081
            - --cert-dir=/etc/paddock-broker/tls
          ports:
            - name: https
              containerPort: 8443
              protocol: TCP
            - name: probes
              containerPort: 8081
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: probes
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: probes
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tls-cert
              mountPath: /etc/paddock-broker/tls
              readOnly: true
      volumes:
        - name: tls-cert
          secret:
            secretName: broker-serving-cert
            # Omit defaultMode — Kubernetes defaults to 0o644 (decimal
            # 420), which lets the non-root broker process (UID 65532)
            # read tls.crt/tls.key via the "other" bit. An earlier
            # `defaultMode: 0o420` rendered as decimal 272 = 0420
            # octal = r---w---- which root can read but non-root
            # cannot, causing every TLS handshake to fail with
            # "remote error: tls: internal error" once
            # GetCertificate ran LoadX509KeyPair.
