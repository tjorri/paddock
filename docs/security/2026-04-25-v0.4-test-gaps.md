# v0.4 test gap inventory (2026-04-25)

- Status: Phase 1 deliverable; consumed by Phase 2 test work.
- Anchor: [`2026-04-25-v0.4-audit-findings.md`](./2026-04-25-v0.4-audit-findings.md).
- Threat model: [`threat-model.md`](./threat-model.md).

## 1. Threat-coverage matrix

The matrix maps each threat actor (T-N from `threat-model.md` §3) crossed with a STRIDE category to the existing test surface. Cells contain test names + status. `✓ covered` = at least one test asserts the defence. `⚠ partial` = some shape covered but a documented edge case is missing. `× gap → TG-NN` = no test exercises this defence; the gap entry in §3 specifies the scenario.

Test-name conventions:
- Unit: file::TestName, e.g., `proxy/server_test.go::TestProxy_DeniesWhenHostNotInAllowList`.
- Integration: envtest-suite test, e.g., `controller/suite_test.go` (Ginkgo `Describe/It`).
- E2E: scenario name from `test/e2e/e2e_test.go`, e.g., `e2e v0.3 hostile prompt egress-block`.

| Threat × STRIDE | Unit | Integration (envtest) | E2E |
|-----------------|------|------------------------|-----|
| T-1.S Agent forges proxy CA-signed cert (B-4) | × gap → TG-1 | × gap | × gap |
| T-1.T Agent unsets HTTPS_PROXY (cooperative-bypass, B-4) | × gap | × gap | × gap → TG-2 |
| T-1.T Agent overrides `SSL_CERT_FILE` / `NODE_EXTRA_CA_CERTS` via `extraEnv` (F-39, B-4) | × gap → TG-3 | × gap | × gap → TG-3 |
| T-1.T Agent connects via IP literal / RFC1918 to skip iptables-redirect (F-20, B-4) | × gap | × gap | × gap → TG-4 |
| T-1.S Agent attempts DNS rebinding to allowlisted name (F-22, B-5) | × gap → TG-5 | × gap | × gap → TG-5 |
| T-1.I Agent reads CA private key from filesystem (F-18, B-4) | × gap → TG-6 | × gap | × gap → TG-6 |
| T-1.E Agent forges AuditEvent via collector SA token (F-38, B-2) | × gap → TG-7 | × gap | × gap → TG-7 |
| T-1.I Agent reads other-tenant PVC / files (T-4, B-2) | × gap | × gap | × gap → TG-8 |
| T-1.D Agent floods proxy goroutines / FDs (F-26, B-4) | × gap → TG-9 | × gap | × gap |
| T-1.D Agent pins memory via distinct SNI cache (F-28, B-4) | × gap → TG-9 | × gap | × gap |
| T-1.I Agent smuggles headers / query params past substitute-auth (F-21, B-5) | `proxy/substitute_test.go::TestProxy_SubstituteAuthRewritesHeaders` ⚠ | × gap | × gap → TG-10 |
| T-1.E Agent calls broker direct, bypassing proxy (F-20, B-3) | `broker/server_test.go::TestIssue_CrossNamespaceDenied` ⚠ | × gap | × gap → TG-11 |
| T-1.S Agent calls IMDS (`169.254.169.254`) (F-23, F-34, B-5) | × gap → TG-12 | × gap | × gap → TG-12 |
| T-1.S Agent CONNECTs to `*.svc.cluster.local` / `localhost` (F-23, F-34) | partial: `proxy/server_test.go::TestProxy_DeniesWhenHostNotInAllowList` ⚠ | × gap | × gap → TG-12 |
| T-1.E Agent uses substituted credential against non-allowed host (F-09, B-5) | `broker/providers/usersuppliedsecret_test.go::TestUserSuppliedSecret_SubstituteAuth_HostMismatchErrors` ⚠ vertical-providers untested | × gap → TG-13 | × gap |
| T-2 Untrusted prompt coerces agent to attempt egress | n/a | n/a | `e2e v0.3 hostile prompt egress-block` ✓ |
| T-3.T Operator writes `appliesToTemplates: ["claude-code-*"]` glob (F-30) | `policy/intersect_test.go` ⚠ no glob coverage | × gap | × gap → TG-14 |
| T-3.T Operator admits cluster-internal egress (`*.svc.cluster.local`, IMDS) (F-34) | × gap → TG-15 | × gap | × gap |
| T-3.T Operator admits IP literal / port-in-host (F-35) | × gap → TG-15 | × gap | × gap |
| T-3.T Operator omits SecurityContext on first-party containers (F-37) | `controller/pod_spec_test.go` ⚠ asserts shape, not PSS-restricted compliance | × gap | × gap → TG-16 |
| T-3.R `extraEnv.valueFrom` smuggles via `fieldRef`/`configMapKeyRef` (F-31) | `webhook/v1alpha1/harnessrun_webhook_test.go::secretKeyRef` ⚠ | × gap → TG-17 | × gap |
| T-4.S Co-tenant presents another tenant's SA token (B-3) | `broker/server_test.go::TestIssue_MissingAuth` ⚠ | × gap | × gap → TG-18 |
| T-4.I Co-tenant reads cluster-shared MITM CA via per-run Secret (F-18) | × gap → TG-6 | × gap | × gap |
| T-5.R Broker swallows AuditEvent write failures (F-12, F-13, B-3) | × gap → TG-19 | × gap | × gap |
| T-5.R Proxy swallows AuditEvent write failures (F-24, B-5) | × gap → TG-19 | × gap | × gap |
| T-6 Supply-chain: `defaultSeedImage` not digest-pinned (F-49) | × gap | × gap | × gap → TG-20 |
| T-7 Webhook self-DoS via cluster-wide `failurePolicy: Fail` (F-33) | × gap | × gap → TG-21 | × gap |
| T-8 Lifecycle: bearer remains valid after run Cancelled (F-10, F-11) | × gap → TG-22 | × gap → TG-22 | partial: `e2e v0.3 BrokerPolicy deleted mid-run` ⚠ asserts new connections blocked, not lease still-valid |
| T-8 Lifecycle: PATPool re-leases on broker restart (F-14) | × gap → TG-22 | × gap | × gap |
| T-8 Lifecycle: `terminationGracePeriodSeconds` unbounded (F-42) | × gap | × gap → TG-23 | × gap |
| Workspace seed: hostile git URL scheme (`file://`/`http://`) (F-46) | × gap → TG-24 | × gap | × gap → TG-24 |
| Workspace seed: cleartext credential persisted to PVC (F-50) | × gap → TG-25 | × gap | × gap → TG-25 |
| Workspace seed: NetworkPolicy missing on seed Pod (F-45) | `controller/network_policy_test.go::TestBuildRunNetworkPolicy_Shape` ⚠ run-only, no seed coverage | × gap | × gap → TG-24 |
| Workspace seed: seed Pod automounts default SA token (F-48) | × gap → TG-7 | × gap | × gap |
| CLI: `logs --file <path>` no workspace-prefix (F-54) | `cli/logs_test.go::TestNewReaderPod_CommandAvoidsShell` ⚠ shell-injection only | × gap → TG-26 | × gap |
| CLI: `logs --reader-image` no registry allowlist (F-55) | × gap → TG-26 | × gap | × gap |

## 2. Per-component coverage

(Populated below.)

## 3. Attack-scenario list

(Populated below.)

## 4. Hostile harness ("evil paddock-echo") specification

(Populated below.)

## 5. Test-realism dimensions

(Populated below.)
